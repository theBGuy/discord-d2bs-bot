version: "3.9"
services:
  discord-socket-bot:
    build: .
    ports:
      - "12345:12345"
    environment:
      - CLIENT_TOKEN=${CLIENT_TOKEN}
      - CLIENT_ID=${CLIENT_ID}
      - CHANNEL_ID=${CHANNEL_ID}
